---
title: Page
metadata:
    helmet:
---
<!doctype html>
<html lang='en'>
    <head>
        <meta charset='UTF-8'>
        <meta name='viewport' content='width=device-width, initial-scale=1.0'>
        <title>Document</title>
        {%- for css in collections.css %}
            <link rel='stylesheet' href='{{ css.data.permalink }}'>
        {%- endfor %}
        {%- if metadata.helmet %}
            {{ helmet }}
        {%- endif %}
    </head>

    <body>
        {{ content }}
    </body>
</html>
